#include <stdio.h>
#include <unistd.h>
int main() {

  printf("Enter programs to run.\n");
  char* buff = NULL;
  size_t bufflen = 0;
  ssize_t programs;
  programs = getline(&buff, &bufflen, stdin);
  if (programs == -1) {
    exit(1);
    free (buff);
  }
}
