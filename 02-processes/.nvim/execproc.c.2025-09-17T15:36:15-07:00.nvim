#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
int main() {
  printf("YAY!\n");
  pid_t pid = fork();
  if (pid == 0) {
    //I am the child
    printf("CHILD!\n");
  } else { 
    printf("PARENT\n");
  }
  printf("end\n");
}
