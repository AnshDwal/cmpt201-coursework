#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
int main() {
  printf("YAY!\n");
  pid_t pid = fork();
  if (pid == 0) {
    // I am the child
    printf("CHILD!\n");
  } else {
    printf("PARENT\n");
    execl("/bin/ls", "/bin/ls", "-a", NULL);
  }
  printf("end\n");
}
